{
  "rules": {
    ".read": false,
    ".write": false,

    "sessions": {
      "$s": {
        ".write": "!newData.exists() && root.child('sessions/' + $s + '/meta/owners/' + auth.uid).val() === true",

        "meta": {
          ".read": "auth != null",
          ".write": "auth != null && (!data.child('owners').exists() || data.child('owners/' + auth.uid).val() === true)",
          "owners": {
            "$uid": { ".validate": "newData.isBoolean()" }
          },
          "romHash": { ".validate": "newData.isString() && newData.val().length <= 128" },
          "romName": { ".validate": "newData.isString() && newData.val().length <= 200" },
          "speedMultiplier": {
            ".write": "root.child('sessions/' + $s + '/controllerLock/holder').val() === auth.uid || root.child('sessions/' + $s + '/meta/owners/' + auth.uid).val() === true",
            ".validate": "newData.isNumber()"
          }
        },

        "invites": {
          ".read": "auth != null",
          "$i": {
            ".write": "auth != null && ( ( !data.exists() && newData.exists() && root.child('sessions/' + $s + '/meta/owners/' + auth.uid).val() === true ) || ( data.exists() && !data.child('redeemedBy').exists() && newData.child('redeemedBy').val() === auth.uid ) || ( !newData.exists() && root.child('sessions/' + $s + '/meta/owners/' + auth.uid).val() === true ) )",
            "createdBy": { ".validate": "newData.isString()" },
            "redeemedBy": { ".validate": "newData.val() === auth.uid" }
          }
        },

        "members": {
          ".read": "root.child('sessions/' + $s + '/members/' + auth.uid).exists()",
          "$uid": {
            ".write": "( $uid === auth.uid && newData.exists() && ( root.child('sessions/' + $s + '/meta/owners/' + auth.uid).val() === true || root.child('sessions/' + $s + '/invites/' + newData.child('viaInvite').val() + '/redeemedBy').val() === auth.uid ) ) || ( $uid === auth.uid && !newData.exists() ) || ( !newData.exists() && root.child('sessions/' + $s + '/meta/owners/' + auth.uid).val() === true )",
            "name": { ".validate": "newData.isString() && newData.val().length <= 64" }
          }
        },

        "controllerLock": {
          ".read": "root.child('sessions/' + $s + '/members/' + auth.uid).exists()",
          "holder": {
            ".write": "root.child('sessions/' + $s + '/meta/owners/' + auth.uid).val() === true || ( root.child('sessions/' + $s + '/members/' + auth.uid).exists() && ( ( newData.val() === auth.uid && ( !data.exists() || data.val() === null ) ) || ( data.val() === auth.uid && ( newData.val() === null || newData.val() === auth.uid ) ) ) )"
          },
          "queue": {
            ".write": "root.child('sessions/' + $s + '/members/' + auth.uid).exists() || root.child('sessions/' + $s + '/meta/owners/' + auth.uid).val() === true"
          },
          "updatedAt": {
            ".write": "root.child('sessions/' + $s + '/members/' + auth.uid).exists() || root.child('sessions/' + $s + '/meta/owners/' + auth.uid).val() === true"
          }
        },

        "sync": {
          ".read": "root.child('sessions/' + $s + '/members/' + auth.uid).exists()",
          "inputs": {
            ".write": "( root.child('sessions/' + $s + '/members/' + auth.uid).exists() && root.child('sessions/' + $s + '/controllerLock/holder').val() === auth.uid ) || root.child('sessions/' + $s + '/meta/owners/' + auth.uid).val() === true",
            "$id": { "by": { ".validate": "newData.val() === auth.uid" } }
          },
          "speed": {
            ".write": "( root.child('sessions/' + $s + '/members/' + auth.uid).exists() && root.child('sessions/' + $s + '/controllerLock/holder').val() === auth.uid ) || root.child('sessions/' + $s + '/meta/owners/' + auth.uid).val() === true",
            "$id": { "by": { ".validate": "newData.val() === auth.uid" } }
          },
          "snapshot": {
            ".write": "( root.child('sessions/' + $s + '/members/' + auth.uid).exists() && root.child('sessions/' + $s + '/controllerLock/holder').val() === auth.uid ) || root.child('sessions/' + $s + '/meta/owners/' + auth.uid).val() === true",
            "by": { ".validate": "newData.val() === auth.uid" }
          }
        },

        "saves": {
          ".read": "root.child('sessions/' + $s + '/members/' + auth.uid).exists()",
          ".write": "root.child('sessions/' + $s + '/members/' + auth.uid).exists()"
        }
      }
    }
  }
}
